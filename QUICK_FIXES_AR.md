# إصلاحات سريعة - اختبارات E2E

## 🔴 يجب إصلاحها فوراً

### 1. نماذج تسجيل الدخول والتسجيل
**المشكلة**: حقول النموذج لا تحتوي على labels

**الحل**:
```tsx
<label htmlFor="email">Email / البريد الإلكتروني</label>
<input id="email" type="email" aria-label="Email" />
```

### 2. الصفحات المحمية (Protected Routes)
**المشكلة**: لا تعيد التوجيه إلى صفحة Login

**الحل**:
```tsx
// ProtectedRoute.tsx
if (!isAuthenticated) {
  return <Navigate to="/login" replace />;
}
```

### 3. تبديل الثيم (Theme Toggle)
**المشكلة**: لا يتغير الثيم عند النقر

**الحل**:
```tsx
const toggleTheme = () => {
  const newTheme = theme === 'light' ? 'dark' : 'light';
  setTheme(newTheme);
  document.documentElement.classList.toggle('dark');
};
```

### 4. أزرار التسعير
**المشكلة**: لا توجه إلى صفحة التسجيل

**الحل**:
```tsx
<Link to="/register?plan=basic">
  <Button>Get Started</Button>
</Link>
```

---

## 🟡 يمكن إصلاحها لاحقاً

### 5. بطاقات التسعير
**الحل**: أضف `data-testid="pricing-card"`

### 6. قسم Features
**الحل**: أضف `data-testid="features-section"`

---

## ✅ ما يعمل بشكل صحيح

- ✅ تحميل جميع الصفحات
- ✅ التنقل بين الصفحات
- ✅ تغيير اللغة
- ✅ الاستجابة للموبايل
- ✅ لا توجد أخطاء في Console

---

## 📊 النتائج

| الاختبار | النجاح | الفشل | النسبة |
|---------|--------|-------|--------|
| Smoke | 10 | 0 | 100% ✅ |
| Landing | 5 | 4 | 56% ⚠️ |
| Auth | 5 | 7 | 42% ⚠️ |
| Pricing | 9 | 2 | 82% 🟡 |

---

## 🚀 الأوامر

```bash
# تشغيل جميع الاختبارات
npm run test:e2e

# تشغيل اختبار محدد
npx playwright test e2e/landing.spec.ts

# عرض التقرير
npm run test:e2e:report
```
