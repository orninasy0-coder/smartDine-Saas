/**
 * Restaurants Management Page Example
 * Demonstrates the restaurants management interface with sample data
 */

import { Restaurants } from './Restaurants';

export default function RestaurantsExample() {
  return <Restaurants />;
}
